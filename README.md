# iceclient
 A Minecraft mod that implements elytra fly hacks through manipulating client side player velocity vectors as well as approximately 30% faster block breaking by sending block break packets as soon as the player begins mining, instead of when it actually should break. These can be toggled using /infiniteFly and /fastBreak respectively.
